[tool.pytest.ini_options]
# Adds project root to sys.path so 'from src.xxx import ...' works in CI.
# More reliable than PYTHONPATH env var or conftest.py sys.path tricks.
pythonpath = ["."]
testpaths = ["tests"]
addopts = "--tb=short"
